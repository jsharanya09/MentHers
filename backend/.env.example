PORT=3001
APP_URL=http://localhost:5173

# Email (optional). Leave SMTP_HOST empty to only print emails in the terminal.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=MentHers <no-reply@example.com>
